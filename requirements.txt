# Inference-only dependencies for the Targeted ABSA API server.
#
# Two-step install (CPU-only torch is 300 MB vs 2.5 GB for CUDA torch):
#   Step 1: pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
#   Step 2: pip install -r requirements.txt
#
# (CPU torch is important for demo deployment — avoids a 2.5 GB download)

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic>=2.7.0
torch>=2.1.0
transformers==4.44.2
sentencepiece==0.2.0
protobuf==4.25.3
pyyaml==6.0.2
numpy>=1.26.4
